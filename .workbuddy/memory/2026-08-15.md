# 2026-08-15 工作日志

## 美化 H_Tool 主页并发布到资料库
- 重构了 H_Tool 工具集导航主页（`index.html` + `assets/main.css` + `assets/common.js` + `assets/links.json`）。
- 设计方向：翠绿品牌色（呼应原有薄荷绿）、Hero 极光背景、精致工具卡片（emoji 图标 + 分类标签 + 强调色 + 悬停箭头）、深浅色主题切换（localStorage 记忆）、卡片入场错峰动画、响应式、WCAG 无障碍（focus-visible / 对比度 / reduced-motion）。
- 关键决策：用 emoji 图标替代原 `links.json` 引用的 `assets/pic/2.png`、`3.png`（这两个文件实际缺失，原页面本就不渲染图片），改为 `icon/tag/accent/comingSoon` 字段，common.js 内嵌兜底数据以便 file:// 直接打开也能显示。
- 全站零外部依赖（原生 CSS/JS），确保资料库沙箱内可正常显示。
- 发布：client 模式鉴权后 `page/import_html.py` 导入整站 zip（排除 .git/.workbuddy），节点 `rfF5Lx0MmhnVTCHc969uX0`，链接 https://www.workbuddy.cn/space/d/rfF5Lx0MmhnVTCHc969uX0 。未公开发布（publish_url 为空），如需公开分享可让我执行 publish。
- 本地预览服务器：http://127.0.0.1:8765/index.html （后台 python http.server）。
